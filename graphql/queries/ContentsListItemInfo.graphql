query ContentsListItemInfo(
  $userId: String!
  $documentId: String!
  $days: Int!
) {
  Creator {
    getNDaysRoyalty(documentId: $documentId, days: $days) {
      activeDate
      documentId
      royalty
      pageview
      totalPageview
    }
  }
  UserDocumentFavorite {
    findOne(filter: { userId: $userId, documentId: $documentId }) {
      documentId
    }
  }
}
