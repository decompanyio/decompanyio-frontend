version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10 # 런타임 nodejs10으로 설정
    commands:
      - npm i npm@latest -g # npm 버전 업데이트
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws --version
      - $(aws ecr get-login --region us-west-1 --no-include-email)  # AWS ECR 로그인
      - REPOSITORY_URI=197966029048.dkr.ecr.us-west-1.amazonaws.com/decompany/frontend-dev  # REPOSITORY_URI 설정
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7) # COMMIT_HASH 설정
      - IMAGE_TAG=${COMMIT_HASH:=latest}  # IMAGE_TAG 설정
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
      - docker build -t $REPOSITORY_URI:latest .
      - docker tag $REPOSITORY_URI:latest $REPOSITORY_URI:$IMAGE_TAG
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker images...
      - docker push $REPOSITORY_URI:latest
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - echo Writing image definitions file...
      - printf '[{"name":"frontend-dev","imageUri":"%s"}]' $REPOSITORY_URI:$IMAGE_TAG > imagedefinitions.json
artifacts:
  files: imagedefinitions.json